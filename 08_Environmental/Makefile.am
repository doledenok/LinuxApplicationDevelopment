AM_CFLAGS = -O0 -g

bin_PROGRAMS=rhasher

rhasher_SOURCES=src/rhasher.c

TESTS=test.sh

distclean:
	rm -rf *.cache *.log config.* configure rhasher *.o *.m4 install-sh Makefile Makefile.in missing stamp-h1 compile depcomp test-driver